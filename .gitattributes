# 저장소와 작업 트리 모두 LF 로 고정한다.
# 이유: 로컬(Windows)이 CRLF, CI(Linux)가 LF 면 prettier --check 가 영구히 어긋난다.
* text=auto eol=lf

# 바이너리는 손대지 않는다
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
